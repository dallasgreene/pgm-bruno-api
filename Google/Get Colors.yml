info:
  name: Get Colors
  type: http
  seq: 6

http:
  method: GET
  url: "{{calendar-api-uri}}/colors"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
